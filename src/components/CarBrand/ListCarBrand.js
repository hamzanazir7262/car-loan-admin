import React from 'react';
import { Box, Typography } from '@mui/material';

const ListCarBrand = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">List Car Brand</Typography>
      {/* Add dashboard content here */}
    </Box>
  );
};

export default ListCarBrand;