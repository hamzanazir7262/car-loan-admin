import React from 'react';
import { Box, Typography } from '@mui/material';

const ListCarType = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">List Car Type</Typography>
      {/* Add dashboard content here */}
    </Box>
  );
};

export default ListCarType;