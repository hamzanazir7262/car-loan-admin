import React from 'react';
import { Box, Typography } from '@mui/material';

const ListCity = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">List City</Typography>
      {/* Add dashboard content here */}
    </Box>
  );
};

export default ListCity;