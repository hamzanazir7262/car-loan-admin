import React from 'react';
import { Box, Typography } from '@mui/material';

const ListCar = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">List Car</Typography>
      {/* Add dashboard content here */}
    </Box>
  );
};

export default ListCar;
