import React from 'react';
import { Box, Typography } from '@mui/material';

const ListGallery = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">List Gallery</Typography>
      {/* Add dashboard content here */}
    </Box>
  );
};

export default ListGallery;
