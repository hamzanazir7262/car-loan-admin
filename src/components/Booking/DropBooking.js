import React from 'react';
import { Box, Typography } from '@mui/material';

const DropBooking = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Drop Booking</Typography>
      {/* Add dashboard content here */}
    </Box>
  );
};

export default DropBooking;
