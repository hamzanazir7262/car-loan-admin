import React from 'react';
import { Box, Typography } from '@mui/material';

const CompletedBooking = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Completed Booking</Typography>
      {/* Add dashboard content here */}
    </Box>
  );
};

export default CompletedBooking;
