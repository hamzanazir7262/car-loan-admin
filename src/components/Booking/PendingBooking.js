import React from 'react';
import { Box, Typography } from '@mui/material';

const PendingBooking = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Pending Booking</Typography>
      {/* Add dashboard content here */}
    </Box>
  );
};

export default PendingBooking;
