import React from 'react';
import { Box, Typography } from '@mui/material';

const PickBooking = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Pick Booking</Typography>
      {/* Add dashboard content here */}
    </Box>
  );
};

export default PickBooking;
